# 贡献指南

感谢你愿意参与思维小岛。这个项目面向小学阶段数学思维训练，当前优先服务 1-3 年级孩子；任何功能和题目都应优先考虑清晰、安全、低压力和适龄。

## 可以贡献什么

- 新增 1-3 年级友好的数学思维题目，当前优先补充一年级基础路线
- 改进题目解释，让孩子更容易理解
- 增加错题复习、学习报告、每日任务等功能
- 改进无障碍体验，例如更大的文字、更好的键盘操作和朗读支持
- 修复界面、逻辑或浏览器兼容问题

## 题目设计原则

- 题干短，避免复杂阅读负担
- 题目训练思维，不只训练机械计算
- 每道题都要提供清楚的解题说明
- 不收集孩子的个人信息
- 不使用容易造成焦虑的排名、惩罚或失败文案

## 本地开发

当前版本是静态 Web 应用，直接打开 `index.html` 即可运行。
本地预览、检查和测试都只需要 Node.js，不需要安装额外 npm 包。

题库位于 `data/lessons.js`。如果只贡献题目，通常不需要修改 `app.js`。

提交改动前建议检查：

```powershell
npm run check
npm test
```

如果 Windows PowerShell 拦截 `npm.ps1`，可以改用：

```powershell
npm.cmd run check
npm.cmd test
```

如果修改题库，请确认：

- 每道题都有稳定的 `id`
- `id` 使用小写英文 slug，并在同一关卡内唯一
- 每个关卡都包含 `grade`、`gradeBand`、`domain` 和 `level`
- 每道题的 `answer` 都存在于 `choices` 中
- 题干、解释、选项和视觉提示符合长度限制
- 没有重复选项或空白视觉提示

稳定题目 ID 会用于错题复习、进度追踪和未来学习报告。

新增题目前，请先阅读 [题目作者指南](docs/QUESTION_AUTHOR_GUIDE.md)。

## 提交建议

- 一个 Pull Request 只解决一个明确问题
- 标题说明用户价值，例如“增加错题复习入口”
- 如果改动界面，请附上截图或说明验证方式
- 如果新增题目，请说明训练目标，例如数感、规律、比较、应用题或空间观察
